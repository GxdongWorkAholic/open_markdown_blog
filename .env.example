# 方案 B 加密门禁 —— 构建期加密脚本使用的访问令牌（约 256 位随机串）
#
# 用法（二选一）：
#   1) 复制本文件为 .env.local（已被 .gitignore 忽略），填入真实 token：
#        ACCESS_TOKEN=<256 位随机串>
#      然后运行：npm run encrypt
#   2) 直接在命令行注入环境变量：
#        ACCESS_TOKEN=<256 位随机串> node scripts/encrypt-config.mjs
#
# 注意：
#   - token 本体绝不能提交进仓库；只有脚本生成的 src/secure/config.enc.json（密文）入库。
#   - 轮换 key：重新生成 token → 重跑 npm run encrypt → 重新构建。旧链接随即失效。
#   - 运行时用户通过 ?key=<token> 访问；Web Crypto 需 https 或 localhost 安全上下文。
ACCESS_TOKEN=
